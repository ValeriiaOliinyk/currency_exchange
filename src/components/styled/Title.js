import styled from "styled-components";

export default styled.h2`
  text-align: center;
  margin-bottom: 20px;
  color: ${(props) => props.theme.colors.text};
  font-weight: 700;
  font-size: 28px;
`;
